# 计划 · 文学 LiteratureAdvisor · 20260903

> 计划时间:2026-09-03 03:10(CST) · 计划人:08-文学-Literature 顾问
> 制定依据:`.Log/巡检-文学-20260903.md` 第 4 节 T1-T5 行动建议
> 止损目标:连续 9 日无计划文件 → 今日恢复 `YYYYMMDD.md` 命名

---

## 一、今日 4 件实事(按优先级)

| # | 实事 | 阶段 | 落地位置 | 优先级 |
|---|------|------|----------|--------|
| 1 | **Phase 0 作品 11 → 30**(先秦 / 唐诗 / 宋词 / 元曲 / 明清小说 / 现代文学 / 西方经典 7 块各补 3-4 部) | Phase 0 | `data/works.json` | P0 |
| 2 | **Phase 0 作家 20 → 50**(每个时期补到 4-5 位,优先补唐诗 / 宋词 / 西方 19 世纪) | Phase 0 | `data/authors.json` | P0 |
| 3 | **Phase 1 第 5 步:`frontend/` 目录 + React + TypeScript + Vite 骨架**(5 文件:`package.json` / `tsconfig.json` / `vite.config.ts` / `src/main.tsx` / `src/App.tsx`,跑 `npm run dev` 起 5173 端口与 FastAPI 8000 CORS 打通) | Phase 1 | `frontend/` 目录 | P1 |
| 4 | **Phase 1 第 6 步:`app/analyze_service.py` LLM 接入骨架**(`/analyze/{work_id}` 从读静态 JSON 改为读 works.json + 调 LLM 模板,`pydantic.BaseModel` 强约束,失败回退 v0.2.0 静态 JSON) | Phase 1 | `app/analyze_service.py` | P1 |

---

## 二、阶段状态(20260903 巡检基线)

### 2.1 Phase 0 资产盘点(7 项,4/7 = 57.1%)

- [x] 盘点 10 大主题 md 建索引
- [x] 仓库维护清理合并冲突标记
- [ ] 提取 500+ 经典作品 → `works.json`
  - [x] 种子 11 部(2026-08-25,**连续 9 日未动**)
  - [ ] 扩到 100 部 ← **今日 P0 实事 1:11 → 30**
  - [ ] 扩到 500 部
- [ ] 提取 200+ 作家 → `authors.json`
  - [x] 种子 20 作家(2026-08-28,**连续 7 日未动**)
  - [ ] 扩到 200 作家 ← **今日 P0 实事 2:20 → 50**
- [ ] 素材库初版 5000+ 条
- [x] 文本精读模板 v1.0(5 维)
- [ ] SQLite → PostgreSQL + Neo4j 迁移

### 2.2 Phase 1 MVP(7 项,4/7 = 57.1%)

- [x] FastAPI 骨架 + 基础 4 GET 接口(v0.1.0,2026-08-29)
- [x] `/analyze/{work_id}` 精读接口(v0.2.0,2026-08-31)
- [ ] React 前端:作品浏览 + 精读页 + 写作反馈 ← **今日 P1 实事 3:frontend/ React 骨架**
- [ ] 文本精读(模板 + LLM)← **今日 P1 实事 4:analyze_service.py LLM 接入**
- [x] 写作反馈基础 5 维评分(v0.4.0-phase1-feedback,2026-09-02 ✅ 已闭环,0903 保持)
- [ ] 素材库检索
- [ ] 飞书 OAuth 登录
- [x] Docker Compose 一键启动(v0.3.0-deploy,2026-09-01)

---

## 三、今日排程建议(给 T1-T5 流程)

| 任务 | 建议动作 |
|------|----------|
| **T1 拉 issues** | 关闭 1 个已闭环 issue(/feedback 写作反馈骨架 v0.4.0,commit `04a94d4`);新增 4 issue:Phase 0 作品 11→30 / 作家 20→50 / Phase 1 第 5 步 frontend/ React 骨架 / Phase 1 第 6 步 analyze_service LLM 接入 |
| **T2 改 bug** | `.gitignore` 从 98 → ~250 字节,补齐 `dist/` `build/` `*.pyc` `*.egg-info/` `.env*` `coverage/` `*.sqlite` `frontend/dist/` `frontend/node_modules/`(0829 已加全局 `node_modules/`,需细分) |
| **T3 加 API** | (a) `frontend/` 5 文件 React+TS+Vite 骨架,`package.json` 引入 `react` / `react-dom` / `react-router-dom` / `vite` / `@vitejs/plugin-react` / `typescript`;(b) `app/analyze_service.py` LLM 接入骨架,`pydantic.BaseModel` 强约束 5 维 JSON,失败回退 v0.2.0 静态 JSON;`app/main.py` 9,071B → ~11,000B;`requirements.txt` 不变 |
| **T4 优化** | ✅ 本文件 `.plan/20260903.md`(止损 9 日计划文件缺失);`项目开发计划.md` 不动(4/7 checkbox 已是当前真实状态) |
| **T5 推 release** | 当前可推 `v0.4.0-phase1-feedback` 标签(0902 闭环标记),如 T3 落地 frontend + analyze LLM 可推 `v0.5.0-phase1-frontend`;`/analyze/{work_id}` 仍是 v0.2.0 静态 JSON 闭环,Docker 部署 v0.3.0 闭环 |

---

## 四、今日选做 1 件(本 cron 触发)

按 cron 触发节奏,本 turn 实际做 1 个小变更:

- **✅ 选 1:生成本计划文件**(`.plan/20260903.md`),止损 9 日无计划文件
- 留待后续 cron 触发分步推进:Phase 0 作品 11→30(可能跨 2-3 日)/ 作家 20→50(可能跨 2-3 日)/ frontend/ 骨架(可能跨 1-2 日)/ analyze_service LLM(可能跨 1-2 日)

---

## 五、风险与边界

- **连续卡点未消**:作品库 9 日 / 作家库 7 日不动,直接制约 Phase 1 /analyze 批量消费(目前仅红楼梦 1 部可精读)
- **Phase 1 后 3 主轴空转**:React / 文本精读 LLM / 素材检索 + 1 横切 OAuth,任何 1 项落地都能让 Phase 1 进入 5/7
- **数据真实性纪律**:`data/works.json` / `data/authors.json` 扩量前先与 `_LiteratureLib/04-06` 经典作品赏析 / 作家索引对齐,避免胡编

---

## 六、变更记录

| 日期 | 动作 | commit |
|------|------|--------|
| 2026-09-03 03:10 | 生成本计划文件 `.plan/20260903.md`,止损 9 日缺失 | (本 cron 待 commit) |
