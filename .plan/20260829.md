# 2026-08-29 T4 计划 · Phase 1 起步

> 落地"Phase 0 已收尾,Phase 1 启动"——一个最小可入库 commit

## 今日目标(1 件实事)

**Phase 1 FastAPI 骨架落地** — 止损 5 日零推进

## 任务清单

1. 创建 `app/main.py` —— FastAPI 入口
   - `GET /` — 健康检查 + 服务信息
   - `GET /works` — 读 `data/works.json` 返回作品列表(支持 `?genre=poetry` 过滤)
   - `GET /works/{work_id}` — 按 id 查单部
   - `GET /authors` — 读 `data/authors.json` 返回作家列表(支持 `?dynasty=唐代` 过滤)
   - `GET /authors/{author_id}` — 按 id 查单作家
2. 创建 `requirements.txt` —— fastapi + uvicorn
3. 创建 `app/__init__.py` —— 空包标识
4. 更新 `项目开发计划.md` Phase 1 第一个 checkbox: `- [ ]` → `- [x]`(FastAPI 骨架 + /works /authors 双接口)
5. `git add -A` → commit → push Gitee → push GitHub

## 验收

- `python -c "import app.main"` 无错
- `uvicorn app.main:app --port 8000` 可启动
- `curl localhost:8000/works` 返回 11 部作品
- `curl localhost:8000/authors` 返回 20 位作家

## 不做

- 不改 Phase 0 数据文件(11 作品 / 20 作家已稳定)
- 不上 React 前端(下一 commit)
- 不接数据库(下一阶段)
- 不接 LLM(下一阶段)
