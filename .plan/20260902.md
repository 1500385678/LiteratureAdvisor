# 当日计划 · 文学 LiteratureAdvisor · 20260902

> 计划时间:2026-09-02 03:10(CST)
> 计划人:08-文学-Literature 顾问 · T4 计划阶段产出
> 关联巡检:`.Log/巡检-文学-20260902.md`(连续 8 日无计划文件,本计划止损)

---

## 一、巡检回顾(20260902 02:10)

- ✅ Phase 1 第 3 步 Docker Compose v0.3.0-deploy 已闭环(`6066c06`)
- ❌ Phase 0 作品 11/500(连续 8 日未动)
- ❌ Phase 0 作家 20/200(连续 6 日未动)
- ❌ Phase 1 后 4 子项空转:React / 文本精读 LLM / 写作反馈 / 素材检索 / 飞书 OAuth
- ⚠️ `.plan/` 连续 8 日缺今日计划文件

## 二、今日实事(1 件,可入库)

### 实事 1:Phase 1 第 4 步 · `/feedback` 写作反馈骨架(POST + GET 估评占位)

**目标**:补齐 Phase 1 第 4 子项,让写作反馈接口先有骨架(不调 LLM),Phase 1 进度 3/7 → 4/7(57.1%)。

**范围**:
- `app/main.py` 新增 `/feedback` POST 路由
  - 入参:`{text: str, user_id: str}`(Pydantic BaseModel)
  - 响应:5 维评分占位 JSON(风格 / 结构 / 语言 / 情感 / 可读性,各 0-100,实现为基于 `len(text)` 的简单启发式 — 让 0 分/满分至少有点变化,纯占位数据不可信,前端可渲染)
  - 元字段:`feedback_id`(uuid 前 8 位)、`created_at`(ISO)、`text_length`、`word_count`
- `app/main.py` 新增 `/feedback/{feedback_id}` GET 路由
  - 内存 dict 存最近 50 条 POST 结果(进程内,重启即失,占位存储)
  - 找不到则 404
- `app/main.py` 根 `/` 的 `endpoints` 列表更新,把 `/feedback` `/feedback/{id}` 加进去
- `app/main.py` 顶 docstring + `version="0.4.0-phase1-feedback"` 升级
- `项目开发计划.md` Phase 1 第 4 子项「写作反馈(基础 5 维评分)」checkbox 勾选

**不做**:
- 不调 LLM(占位评分即可,LLM 接入在下一步)
- 不写数据库(内存 dict 即可)
- 不做鉴权(留 Phase 1 第 7 步 OAuth)
- 不写单测(留 Phase 2 接入 PostgreSQL 时一起)

**验收**:
- `python -c "from app.main import app; print([r.path for r in app.routes if 'feedback' in r.path])"` 输出 `['/feedback', '/feedback/{feedback_id}']`
- 启动 `uvicorn app.main:app` 后 `curl -X POST localhost:8000/feedback -d '{"text":"测试文本","user_id":"u1"}' -H 'Content-Type: application/json'` 返回 5 维 JSON

**commit**:`feat(phase-1): /feedback 写作反馈骨架(5 维评分占位)v0.4.0-phase1-feedback`

## 三、推进表

| 项 | 前 | 后 |
|----|----|----|
| Phase 0 checkbox | 4/7(57.1%) | 4/7(57.1%)·未动 |
| Phase 1 checkbox | 3/7(42.9%) | 4/7(57.1%) |
| `app/main.py` 行数 | 160 | ~210(+50) |
| `app/main.py` 接口数 | 5 GET | 5 GET + 1 POST + 1 GET(= 7 routes) |
| `version` | 0.2.0 | 0.4.0-phase1-feedback |

## 四、剩余实事(给后续日期,不入今日)

1. Phase 0 作品 11 → 30(先秦 / 唐诗 / 宋词 / 元曲 / 明清 / 现代 / 西方 7 块各补 3-4 部)
2. Phase 0 作家 20 → 50(每个时期补 4-5 位)
3. Phase 1 第 5 步 · `frontend/` 目录与 React 骨架
4. Phase 1 第 6 步 · 文本精读 LLM 接入(`analyze/` 服务,替手工精读)
5. Phase 1 第 7 步 · 飞书 OAuth(`/auth/` 服务)
6. 素材库 5000 条 + `material/` 服务
7. SQLite → PostgreSQL + Neo4j 迁移脚本
